<!-- Scripts communs -->
<script src="/js/main.js"></script>

<!-- Scripts spécifiques à la page -->
<% if (locals.jsFiles && jsFiles.length > 0) { %>
    <% jsFiles.forEach(function(jsFile) { %>
        <script src="/js/<%= jsFile %>"></script>
    <% }); %>
<% } %>
